import { jsx as _jsx } from "react/jsx-runtime";
import { dragX, dragY, dragData } from "./global.js";
import { useSelector } from "../signal/signal.js";
import { useEffect, useState } from "react";
export function ReactPlaceholder({ Render, isDragging, }) {
    if (!isDragging)
        return;
    return _jsx(ReactPlaceholderImpl, { Render: Render });
}
function ReactPlaceholderImpl({ Render }) {
    const x = useSelector(dragX);
    const y = useSelector(dragY);
    const data = useSelector(dragData);
    const [mount, setMount] = useState(false);
    useEffect(() => {
        setTimeout(() => {
            setMount(true);
        }, 100);
    }, []);
    if (!data || !mount)
        return null;
    return _jsx(Render, { data: data, y: y, x: x });
}
