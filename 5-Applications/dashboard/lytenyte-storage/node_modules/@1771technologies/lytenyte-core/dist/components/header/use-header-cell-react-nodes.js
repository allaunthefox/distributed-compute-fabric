import { jsx as _jsx } from "react/jsx-runtime";
import { useMemo } from "react";
import { HeaderRowContext } from "./header-row/context.js";
export function useHeaderCellReactNodes(headerLayoutRows, children) {
    const headerRows = useMemo(() => {
        const rows = [];
        for (let i = 0; i < headerLayoutRows.length; i++) {
            const rowCells = headerLayoutRows[i];
            rows.push(_jsx(HeaderRowContext, { value: i, children: children(rowCells) }, i));
        }
        return rows;
    }, [children, headerLayoutRows]);
    return headerRows;
}
