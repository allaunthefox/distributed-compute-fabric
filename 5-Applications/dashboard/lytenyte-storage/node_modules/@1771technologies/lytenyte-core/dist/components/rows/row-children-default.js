import { jsx as _jsx } from "react/jsx-runtime";
import { memo } from "react";
import { RowFullWidth } from "./row-full-width.js";
import { Row } from "./row/row.js";
import { Cell } from "../cells/cell.js";
export function RowChildrenDefault(row) {
    if (row.kind === "full-width")
        return _jsx(RowFullWidth, { row: row });
    return _jsx(RowWithCellRenderer, { row: row });
}
const RowWithCellRenderer = memo(({ row }) => {
    return (_jsx(Row, { row: row, children: row.cells.map((cell) => {
            return _jsx(Cell, { cell: cell }, cell.id);
        }) }, row.id));
});
