import { jsx as _jsx } from "react/jsx-runtime";
import { HeaderCell } from "../header-cell/header-cell.js";
import { HeaderGroupCell } from "../header-cell/header-group-cell.js";
import { HeaderRow } from "./header-row.js";
export function HeaderRowRenderer(cells) {
    return (_jsx(HeaderRow, { children: cells.map((c) => {
            if (c.kind === "group")
                return _jsx(HeaderGroupCell, { cell: c }, c.idOccurrence);
            return _jsx(HeaderCell, { cell: c }, c.id);
        }) }));
}
