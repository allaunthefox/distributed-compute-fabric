import { jsx as _jsx } from "react/jsx-runtime";
import { createContext, useContext, useMemo, useRef } from "react";
const context = createContext(null);
const contextSettings = createContext(null);
export function SelectPivotProvider(props) {
    const selectPivot = useRef(null);
    const settings = useMemo(() => ({
        selectActivator: props.rowSelectionActivator ?? "single-click",
    }), [props.rowSelectionActivator]);
    return (_jsx(context.Provider, { value: selectPivot, children: _jsx(contextSettings.Provider, { value: settings, children: props.children }) }));
}
export const useSelectPivotRef = () => useContext(context);
export const useRowSelectionSettings = () => useContext(contextSettings);
