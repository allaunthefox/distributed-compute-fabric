import { useRef } from "react";
import { useEvent } from "../../../../hooks/use-event.js";
export function useViewport(vp) {
    const vpRef = useRef(vp);
    vpRef.current = vp;
    return useEvent(() => vpRef.current);
}
