import { jsx as _jsx } from "react/jsx-runtime";
import { createContext, memo, useContext, useId } from "react";
const context = createContext("");
export const GridIdProvider = memo((props) => {
    const id = useId();
    const gridId = props.gridId ?? id;
    return _jsx(context.Provider, { value: gridId, children: props.children });
});
export const useGridIdContext = () => useContext(context);
