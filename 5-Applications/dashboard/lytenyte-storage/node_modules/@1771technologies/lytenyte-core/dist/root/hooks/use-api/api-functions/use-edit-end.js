import { useEvent } from "../../../../hooks/use-event.js";
export function useEditEnd(edit) {
    return useEvent((cancel) => {
        if (cancel) {
            edit.cancel();
            return true;
        }
        return edit.commit();
    });
}
