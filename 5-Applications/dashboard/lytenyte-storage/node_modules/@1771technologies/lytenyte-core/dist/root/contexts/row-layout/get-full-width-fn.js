export function getFullWidthFn(rowByIndex, predicate, api) {
    if (!predicate)
        return null;
    return (r) => {
        const rowNode = rowByIndex(r);
        if (!rowNode)
            return false;
        return predicate({ row: rowNode, rowIndex: r, api });
    };
}
