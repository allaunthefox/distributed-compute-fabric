export interface Event<T> {
    (nextValue: T): void;
    on: (callback: (value: T) => void) => () => void;
}
export interface Signal<T> {
    (): T;
    (nextValue: T | ((value: T) => T)): T;
    on: (callback: (value: T) => void) => () => void;
}
export interface UseSelector {
    <T>(signal: Signal<T>): T;
    <T, K>(signal: Signal<T>, selector: (value: T) => K): K;
    <T>(selector: () => T): T;
}
export declare const createEvent: <T = void>() => Event<T>;
export declare const createSignal: <T>(initialValue: T | (() => T)) => Signal<T>;
export declare const useSelector: UseSelector;
