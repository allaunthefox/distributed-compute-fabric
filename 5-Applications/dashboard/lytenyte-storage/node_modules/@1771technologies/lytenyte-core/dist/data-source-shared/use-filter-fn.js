import { useMemo } from "react";
export function useFilterFn(filter) {
    return useMemo(() => {
        if (!filter)
            return null;
        if (!Array.isArray(filter))
            return filter;
        return (node) => {
            return filter.every((fn) => fn(node));
        };
    }, [filter]);
}
