import { type RowNode, type RowSource } from "@1771technologies/lytenyte-shared";
import type { SourceRowSelection } from "./use-row-selection.js";
import type { Signal } from "../../signal/signal.js";
export declare function useOnRowsSelected<T>(s: SourceRowSelection, idToSpec: (id: string) => {
    size: number;
    children: Map<any, {
        row: RowNode<any>;
    }>;
} | null, rowParents: RowSource<T>["rowParents"], isolatedSelected: boolean, globalSignal: Signal<number>): (params: {
    readonly selected: string[] | "all";
    readonly deselect?: boolean;
    readonly mode: "single" | "multiple" | "none";
}) => void;
