import type { Dispatch, SetStateAction } from "react";
export interface Piece<T> {
    readonly get: () => T;
    readonly useValue: {
        (): T;
        <K>(s: (v: T) => K): K;
    };
}
export interface PieceWritable<T> extends Piece<T> {
    readonly set: Dispatch<SetStateAction<T>>;
}
/**
 * A hook that is capable of creating state that can be scoped.
 */
export declare function usePiece<T>(source: T): Piece<T>;
export declare function usePiece<T>(source: T, setter: Dispatch<SetStateAction<T>>): PieceWritable<T>;
