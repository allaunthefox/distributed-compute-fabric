import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import "../test.css";
import "./navigation.css";
import { Grid, useClientDataSource } from "../../index.js";
import { useState } from "react";
import { bankDataSmall } from "@1771technologies/grid-sample-data/bank-data-smaller";
const baseColumns = [
    { id: "age" },
    { id: "job" },
    { id: "balance" },
    { id: "education" },
    { id: "marital" },
    { id: "default" },
    { id: "housing" },
    { id: "loan" },
    { id: "contact" },
    { id: "day" },
    { id: "month" },
    { id: "duration" },
    { id: "campaign" },
    { id: "pdays" },
    { id: "previous" },
    { id: "poutcome", name: "P Outcome" },
    { id: "y" },
];
const rowDetailRenderer = () => {
    return (_jsxs("div", { children: [_jsx("button", { tabIndex: 0, children: "Detail A" }), _jsx("button", { tabIndex: 0, children: "Detail B" })] }));
};
const rowFullWidthPredicate = (r) => r.rowIndex === 2 || r.rowIndex == 4;
const rowFullWidthRenderer = (r) => {
    if (r.rowIndex === 2)
        return _jsx("div", { children: "Nothing" });
    return (_jsxs("div", { children: [_jsx("button", { tabIndex: 0, children: "A" }), _jsx("button", { tabIndex: 0, children: "B" }), _jsx("button", { tabIndex: 0, children: "C" })] }));
};
export default function RowDetail({ rtl, columns }) {
    const ds = useClientDataSource({
        data: bankDataSmall,
    });
    const [expansions, setExpansions] = useState(new Set(["leaf-2", "leaf-5"]));
    const [marker] = useState({
        on: true,
        width: 60,
        cellRenderer: (p) => _jsx("button", { onClick: () => p.api.rowDetailToggle(p.row.id), children: "+" }),
    });
    return (_jsxs("div", { className: "with-border-cells", children: [_jsx("button", { tabIndex: 0, onClick: () => { }, children: "Top Capture" }), _jsx("div", { style: { width: "100%", height: "90vh", border: "1px solid black" }, children: _jsx(Grid, { gridId: "x", columns: columns ?? baseColumns, rowSource: ds, rtl: rtl, rowDetailExpansions: expansions, onRowDetailExpansionsChange: setExpansions, columnMarker: marker, rowFullWidthPredicate: rowFullWidthPredicate, rowFullWidthRenderer: rowFullWidthRenderer, rowDetailRenderer: rowDetailRenderer }) }), _jsx("button", { tabIndex: 0, onClick: () => { }, children: "Bottom Capture" })] }));
}
if (import.meta.vitest) {
    const { test, expect } = import.meta.vitest;
    const { userEvent } = await import("vitest/browser");
    const { wait, getCellQuery } = await import("../utils.js");
    const { render } = await import("vitest-browser-react");
    test("when row details are present it should be possible to navigate across them", async () => {
        const screen = await render(_jsx(RowDetail, {}));
        const grid = screen.getByRole("grid");
        await wait(100);
        await expect.element(grid).toBeVisible();
        await wait(); // Give the grid a moment to render
        const ourFirstCell = document.querySelector(getCellQuery("x", 0, 0));
        ourFirstCell.focus();
        await expect.element(ourFirstCell).toHaveFocus();
        await userEvent.keyboard("{ArrowDown}");
        await wait();
        await userEvent.keyboard("{ArrowDown}");
        await wait();
        await userEvent.keyboard("{ArrowDown}");
        await wait();
        await expect.element(document.activeElement).toHaveTextContent("Detail A");
        await userEvent.keyboard("{ArrowLeft}");
        await wait();
        await expect.element(document.activeElement).toHaveTextContent("Detail ADetail B");
        await userEvent.keyboard("{ArrowLeft}");
        await wait();
        await expect.element(document.activeElement).toHaveTextContent("Detail B");
        await userEvent.keyboard("{ArrowLeft}");
        await wait();
        await expect.element(document.activeElement).toHaveTextContent("Detail A");
        await userEvent.keyboard("{ArrowDown}");
        await wait();
        await expect.element(document.activeElement).toHaveTextContent("+");
        await userEvent.keyboard("{ArrowDown}");
        await wait();
        await expect.element(document.activeElement).toHaveTextContent("ABC");
        await userEvent.keyboard("{ArrowUp}");
        await wait();
        await expect.element(document.activeElement).toHaveTextContent("+");
        await userEvent.keyboard("{ArrowUp}");
        await wait();
        await expect.element(document.activeElement).toHaveTextContent("Detail A");
        await userEvent.keyboard("{ArrowDown}");
        await wait();
        await expect.element(document.activeElement).toHaveTextContent("+");
        await userEvent.keyboard("{ArrowLeft}");
        await wait();
        await expect.element(document.activeElement).toHaveTextContent("+");
        await userEvent.click(document.activeElement);
        await wait();
        await userEvent.keyboard("{ArrowDown}");
        await expect.element(document.activeElement).toHaveTextContent("Detail A");
    });
}
