import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import "../test.css";
import { bankDataSmall } from "@1771technologies/grid-sample-data/bank-data-smaller";
import { Grid, useClientDataSource } from "../../index.js";
import "./navigation.css";
const baseColumns = [
    { id: "age" },
    { id: "job" },
    { id: "balance" },
    { id: "education" },
    { id: "marital" },
    { id: "default" },
    { id: "housing" },
    { id: "loan" },
    { id: "contact" },
    { id: "day" },
    { id: "month" },
    { id: "duration" },
    { id: "campaign" },
    { id: "pdays" },
    { id: "previous" },
    { id: "poutcome", name: "P Outcome" },
    { id: "y" },
];
const rowFullWidthPredicate = (r) => r.rowIndex === 2 || r.rowIndex == 4;
const rowFullWidthRenderer = (r) => {
    if (r.rowIndex === 2)
        return _jsx("div", { children: "Nothing" });
    return (_jsxs("div", { children: [_jsx("button", { tabIndex: 0, children: "A" }), _jsx("button", { tabIndex: 0, children: "B" }), _jsx("button", { tabIndex: 0, children: "C" })] }));
};
export default function FullWidthRows({ rtl, columns }) {
    const ds = useClientDataSource({
        data: bankDataSmall,
    });
    return (_jsxs("div", { children: [_jsx("button", { tabIndex: 0, onClick: () => { }, children: "Top Capture" }), _jsx("div", { style: { width: "100%", height: "90vh", border: "1px solid black" }, children: _jsx(Grid, { gridId: "x", columns: columns ?? baseColumns, rowSource: ds, rtl: rtl, rowFullWidthPredicate: rowFullWidthPredicate, rowFullWidthRenderer: rowFullWidthRenderer }) }), _jsx("button", { tabIndex: 0, onClick: () => { }, children: "Bottom Capture" })] }));
}
if (import.meta.vitest) {
    const { test, expect } = import.meta.vitest;
    const { userEvent } = await import("vitest/browser");
    const { wait, getCellQuery } = await import("../utils.js");
    const { render } = await import("vitest-browser-react");
    test("full width row navigation should correctly handle the tabbables", async () => {
        const screen = await render(_jsx(FullWidthRows, {}));
        const grid = screen.getByRole("grid");
        await wait(100);
        await expect.element(grid).toBeVisible();
        await wait(); // Give the grid a moment to render
        document.querySelector(`[data-ln-gridid="x"][data-ln-rowtype="full-width"][data-ln-rowindex="2"] > div `).focus();
        await wait();
        await expect.element(document.activeElement).toHaveTextContent("Nothing");
        await userEvent.keyboard("{ArrowLeft}");
        await wait();
        await expect.element(document.activeElement).toHaveTextContent("Nothing");
        await userEvent.keyboard("{ArrowRight}");
        await wait();
        await expect.element(document.activeElement).toHaveTextContent("Nothing");
        document.querySelector(`[data-ln-gridid="x"][data-ln-rowtype="full-width"][data-ln-rowindex="4"] > div `).focus();
        await wait();
        await expect.element(document.activeElement).toHaveTextContent("ABC");
        await userEvent.keyboard("{ArrowRight}");
        await expect.element(document.activeElement).toHaveTextContent("A");
        await userEvent.keyboard("{ArrowRight}");
        await expect.element(document.activeElement).toHaveTextContent("B");
        await userEvent.keyboard("{ArrowRight}");
        await expect.element(document.activeElement).toHaveTextContent("C");
        await userEvent.keyboard("{ArrowRight}");
        await expect.element(document.activeElement).toHaveTextContent("A");
        await userEvent.keyboard("{ArrowLeft}");
        await expect.element(document.activeElement).toHaveTextContent("ABC");
        await userEvent.keyboard("{ArrowLeft}");
        await expect.element(document.activeElement).toHaveTextContent("C");
    });
    test("full width row navigation should correctly handle the tabbables rtl", async () => {
        const screen = await render(_jsx(FullWidthRows, { rtl: true }));
        const grid = screen.getByRole("grid");
        await wait(100);
        await expect.element(grid).toBeVisible();
        await wait(); // Give the grid a moment to render
        document.querySelector(`[data-ln-gridid="x"][data-ln-rowtype="full-width"][data-ln-rowindex="2"] > div `).focus();
        await wait();
        await expect.element(document.activeElement).toHaveTextContent("Nothing");
        await userEvent.keyboard("{ArrowRight}");
        await wait();
        await expect.element(document.activeElement).toHaveTextContent("Nothing");
        await userEvent.keyboard("{ArrowLeft}");
        await wait();
        await expect.element(document.activeElement).toHaveTextContent("Nothing");
        document.querySelector(`[data-ln-gridid="x"][data-ln-rowtype="full-width"][data-ln-rowindex="4"] > div `).focus();
        await wait();
        await expect.element(document.activeElement).toHaveTextContent("ABC");
        await userEvent.keyboard("{ArrowLeft}");
        await expect.element(document.activeElement).toHaveTextContent("A");
        await userEvent.keyboard("{ArrowLeft}");
        await expect.element(document.activeElement).toHaveTextContent("B");
        await userEvent.keyboard("{ArrowLeft}");
        await expect.element(document.activeElement).toHaveTextContent("C");
        await userEvent.keyboard("{ArrowLeft}");
        await expect.element(document.activeElement).toHaveTextContent("A");
        await userEvent.keyboard("{ArrowRight}");
        await expect.element(document.activeElement).toHaveTextContent("ABC");
        await userEvent.keyboard("{ArrowRight}");
        await expect.element(document.activeElement).toHaveTextContent("C");
    });
    test("should be navigate across full width rows", async () => {
        const screen = await render(_jsx(FullWidthRows, {}));
        const grid = screen.getByRole("grid");
        await wait(100);
        await expect.element(grid).toBeVisible();
        await wait(); // Give the grid a moment to render
        const ourFirstCell = document.querySelector(getCellQuery("x", 0, 2));
        ourFirstCell.focus();
        await expect.element(document.activeElement).toHaveTextContent("1787");
        await userEvent.keyboard("{ArrowDown}");
        await expect.element(document.activeElement).toHaveTextContent("4789");
        await userEvent.keyboard("{ArrowDown}");
        await expect.element(document.activeElement).toHaveTextContent("Nothing");
        await userEvent.keyboard("{ArrowDown}");
        await expect.element(document.activeElement).toHaveTextContent("1476");
        await userEvent.keyboard("{ArrowDown}");
        await expect.element(document.activeElement).toHaveTextContent("ABC");
        await userEvent.keyboard("{ArrowDown}");
        await expect.element(document.activeElement).toHaveTextContent("747");
        await userEvent.keyboard("{ArrowUp}");
        await expect.element(document.activeElement).toHaveTextContent("ABC");
        await userEvent.keyboard("{ArrowUp}");
        await expect.element(document.activeElement).toHaveTextContent("1476");
        await userEvent.keyboard("{ArrowUp}");
        await expect.element(document.activeElement).toHaveTextContent("Nothing");
        await userEvent.keyboard("{ArrowUp}");
        await expect.element(document.activeElement).toHaveTextContent("4789");
    });
}
