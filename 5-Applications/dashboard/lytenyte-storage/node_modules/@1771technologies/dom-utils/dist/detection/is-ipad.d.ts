/**
 * Returns true if the current device is an iPad, including iPadOS 13+ devices that report as
 * Mac. Result is cached after the first call.
 */
export declare const isIPad: {
    (): boolean;
    __clear(): void;
};
