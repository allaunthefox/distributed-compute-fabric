interface Brand {
    brand: string;
    version: string;
}
interface UserAgentData {
    brands: Brand[];
    platform: string;
}
declare global {
    interface Navigator {
        userAgentData?: UserAgentData;
    }
}
/**
 * Returns true if the navigator's userAgent string or any userAgentData brand matches the given
 * regular expression.
 */
export declare function testUserAgent(re: RegExp): boolean;
export {};
