/**
 * Returns a memoized version of a boolean-returning function. The cached result can be reset by
 * calling __clear on the returned function.
 */
export declare function cached(fn: () => boolean): {
    (): boolean;
    __clear(): void;
};
