/**
 * Returns the nearest element matching the predicate, starting at the given element and walking
 * up the DOM tree.
 */
export declare function getNearestMatching(start: HTMLElement, matchPredicate: (el: HTMLElement) => boolean): HTMLElement | null;
