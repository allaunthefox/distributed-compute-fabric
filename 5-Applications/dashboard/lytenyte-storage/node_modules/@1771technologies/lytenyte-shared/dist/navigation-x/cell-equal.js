import { equal } from "@1771technologies/js-utils";
export function cellEqual(left, right) {
    return Boolean((left.rowIndex === right.rowIndex && left.colIndex === right.colIndex) ||
        (left.root && right.root && equal(left.root, right.root)));
}
